if content
	each el in content.projects.items
		if el.id == content.key
			.Inshort(class="[ Element ]")
				.Inshort__body
					.Columns__marginal
						span= "Project " + el.teaser.ressort
					.Columns__back
						a.Button.Button--back(href="/en/"+el.ressort+"/")
							span Go back<br/>to Overview
					.Inshort__spacer
					.Inshort__intro.Inshort__intro--copy
						.Heading.Heading--major.Heading--large
							span= el.content.subtitle
							h2= el.content.title
						.Copy.Copy--colored
							p= el.content.intro
					.Inshort__copy
						if el.content.copy
							.Copy
								each item in el.content.copy
									p= item
